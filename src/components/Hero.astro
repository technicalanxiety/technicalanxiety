---
import { siteConfig } from '../config';
import { getPublishedPosts } from '../utils/posts';

const posts = await getPublishedPosts();
const postCount = posts.length;
---

<section class="hero">
  <div class="container">
    <div class="hero-wrapper">
      <h1 class="hero-title">
        Technology's True Cost
      </h1>
      <span class="hero-subtitle">When it breaks. When it breaks us. What keeps me standing.</span>
      
      <div class="hero-buttons">
        <a href="/start-here/" class="button button-primary">
          <ion-icon name="rocket-outline"></ion-icon>
          Start Here
        </a>
        <a href="/azure-topology/" class="button button-topology">
          <ion-icon name="git-network-outline"></ion-icon>
          Explore Topology
        </a>
      </div>
      
      <p class="hero-description" style="text-align: center !important; margin: 0 auto; max-width: 800px;">
        Azure, governance, and platform patterns. I'm taking notes.
      </p>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 1rem 0 !important;
    text-align: center !important;
  }
  
  .hero-wrapper {
    text-align: center !important;
    max-width: 1200px !important;
    margin: 0 auto !important;
    padding: 1rem 0 !important;
  }
  
  .hero-title {
    text-align: center !important;
    margin-bottom: 0.5rem !important;
  }
  
  .hero-subtitle {
    text-align: center !important;
    display: block !important;
    margin-bottom: var(--space-lg) !important;
  }
  
  .hero-buttons {
    text-align: center !important;
    display: flex !important;
    justify-content: center !important;
    gap: 1rem !important;
    margin-top: 2rem !important;
    margin-bottom: 2rem !important;
  }
  
  .hero-description {
    font-size: 1.25rem !important;
    font-weight: 500 !important;
    color: var(--text-secondary) !important;
    line-height: 1.6 !important;
  }
  
  .button-topology {
    position: relative;
    background: linear-gradient(135deg, #0078D4 0%, #00BCF2 100%) !important;
    color: white !important;
    border: 2px solid #00BCF2 !important;
    box-shadow: 0 4px 15px rgba(0, 188, 242, 0.3) !important;
    animation: topologyPulse 2s ease-in-out infinite;
  }
  
  .button-topology:hover {
    transform: translateY(-3px) !important;
    box-shadow: 0 8px 25px rgba(0, 188, 242, 0.5) !important;
  }
  
  @keyframes topologyPulse {
    0%, 100% {
      box-shadow: 0 4px 15px rgba(0, 188, 242, 0.3);
      transform: scale(1);
    }
    50% {
      box-shadow: 0 6px 30px rgba(0, 188, 242, 0.8);
      transform: scale(1.02);
    }
  }
</style>

